<h1>All Courses Available</h1>


<div>
    <ol><% @courses.each_with_index do |course, index|%>
        <li>
        <h3>Course name: <%= course.name%> <%= course.icon%></h3>
        <p>Description: <%= course.description%></p>
        <p>Difficulty Level:
            <% if course.level == 0 %>
            Easy
            <% elsif course.level == 1 %>
            Intermediate
            <% else %>
            Hard
            <% end%></p>
            </li>
        <p>Instructor name: <%= @names[index]%> </p>
    <%end%></ol>

    <div>
        <%# IF Student, show registration status%>
        <% if !@current_user.instructor %>
            <a href="/enrolled"><p>Click here for my enrolled classes </a></p>
        <% else %> <%# IF Instructor, show course_teaching%>
            <a href="/teaching"><p>Click here for my courses </a></p>
        <% end %>
    </div>
</div>
